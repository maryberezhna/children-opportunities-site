-- Eurodesk Opportunity Finder — європейські програми для молоді.
--
-- Джерело мовчить із 18 травня: рядок у реєстрі був, а модуля не існувало.
-- Причина, чому його не написали раніше, — сайт малюється на клієнті:
-- у відповіді по HTTP немає ні програм, ні посилань. Дані віддає власний
-- виклик /search, який відповідає 403 ззовні, але працює зсередини вже
-- відкритої сторінки. Тому це єдине джерело, якому потрібен браузер.
--
-- 533 програми в переліку, з них ~92 з відкритим набором на момент збору.
-- Частина розрахована на 18+; вік відсіює звичайна екстракція.

insert into sources (name, pipeline, adapter, trust_tier, enabled, notes, config)
values (
  'Eurodesk', 'python', 'html', 2, true,
  'Європейські програми для молоді. Потребує браузера (playwright): сайт '
  'малюється на клієнті. Беремо лише секцію open — програми з відкритим '
  'набором; upcoming свідомо пропускаємо, на них ще не подаси.',
  '{"url": "https://programmes.eurodesk.eu", "needs_browser": true}'::jsonb
)
on conflict (name) do update
set enabled = true,
    notes = excluded.notes,
    config = excluded.config;
